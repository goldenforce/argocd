### Check if Database Schemas Verified
function checkDatabaseSchemasVerify()
{
	### Initialize
	checkDatabaseSchemasVerifyStatus="Failed"
	
	if [ -f ${schemas_verified} ]; then
		checkDatabaseSchemasVerifyStatus="Completed"
	fi
	echo "${checkDatabaseSchemasVerifyStatus}"
}
